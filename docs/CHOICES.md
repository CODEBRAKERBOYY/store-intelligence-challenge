# Choices Document

The canonical choices document for this submission is maintained at the repository root:

[../CHOICES.md](../CHOICES.md)

It is also referenced here because the original challenge suggested a `docs/` directory layout. Keeping the root file preserves the acceptance-gate requirement that `CHOICES.md` exists in an obvious location, while this folder mirrors the suggested structure for reviewers who browse by directory.
